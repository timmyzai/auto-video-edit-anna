# 自动粗剪系统 —— 使用指南

给不需要看代码的人看的说明。每一步都配了可以直接复制粘贴给 Claude Code 的提示词。

## 整体流程一览

```
① 在剪映导入素材
        ↓
② 打开 Claude Code，粘贴提示词，开始自动粗剪
        ↓
③（可选，想要更干净的成片时）提供字幕，让 Claude 结合实际内容审核
        （语气词、重复啰嗦、说错重讲、片场闲聊……）
        ↓
④ 查看 Claude 的检查报告，确认要删除的内容
        ↓
⑤ 确认执行，Claude 把结果写入剪映项目
        ↓
⑥ 在剪映里检查效果，导出成片
```

---

## 第一步：在剪映导入素材（手动，剪映里操作）

1. 打开剪映专业版，新建一个项目，起一个清楚的项目名（后面都要用这个名字）。
2. 把原始素材（一个或多个视频文件）导入项目，按时间顺序放到时间线的一条轨道上。
3. **重要**：导入时确认「HDR 转换工具」开关是打开的。这个只能在剪映里手动开，Claude 无法帮你操作。
4. 记住这个项目名字，接下来都要告诉 Claude。

---

## 第二步：打开 Claude Code，开始自动粗剪

**我们不用 Claude Code 的桌面应用，而是在终端里用命令启动它。步骤如下：**

1. 打开「文件资源管理器」，找到这个项目所在的文件夹。
2. 点一下资源管理器最上面的地址栏（点一下会变成可以打字的输入框），输入：
   ```
   code .
   ```
   按回车 —— 这会用 VS Code 打开这个文件夹。
3. VS Code 打开后，按快捷键 `` Ctrl+` ``（反引号，在数字键 1 左边）打开终端，或者点上面菜单「Terminal（终端）」→「New Terminal（新建终端）」。
4. 在终端里输入：
   ```
   claude
   ```
   按回车，等它启动。看到它准备好接收输入之后，就可以开始下面的步骤了。

**接下来，把下面这句话复制粘贴给它**（把「项目名称」换成你剪映里的真实项目名）：

```
帮我对剪映项目「项目名称」做自动粗剪
```

Claude 会自动完成：
- 找到你导入的素材
- 分析每段素材的音量，判断哪里是说话、哪里是静音
- 生成一份"保留片段"清单

**这一步不需要你做任何事，等 Claude 说完成就行。** 默认不需要字幕，速度快。

---

## 第三步（可选）：需要更干净的成片时，用字幕辅助判断

单纯看音量只能判断"哪里有声音、哪里没声音"，判断不了"说的内容有没有用"。如果你发现：

- 有些说得很轻但很重要的话被切掉了
- 成片里有重复啰嗦、说错重讲、忘词重录的地方
- 混进了片场布置、镜头外闲聊之类不该出现的内容

可以用这个功能，让 Claude 结合实际说话内容判断取舍，而不只是看音量。**这一步需要你先在剪映里手动操作一次：**

1. 在剪映里，对着导入的原始素材，用剪映**自带的自动字幕**功能生成字幕（不需要翻译、不需要校对，生成中文原始字幕就行）。
2. 把字幕导出成 SRT 文件，记住文件路径。
3. 回到 Claude Code，复制粘贴：

```
我已经导出字幕SRT，路径是：[把文件路径粘贴在这里]
请用这份字幕做智能判断：
1. 先自动处理语气词（啊、哦、嗯这类没有实际内容的词）
2. 再检查有没有重复啰嗦、说错重讲、忘词重录、片场布置对话、无关闲聊等应该删除的内容
3. 你自己也拿不准的地方，单独列出来问我，不要直接删
4. 最后检查一下有没有该保留却被误删的重要内容
```

Claude 会重新生成一份更准确的"保留片段"清单，并生成检查报告（见下一步）。**这一步可能需要 Claude 逐句读完整份字幕来判断，内容越多花的时间也越长——耐心等它做完，不用催，它会随时告诉你进度到哪了。**

---

## 第四步：查看检查报告，决定删哪些

Claude 会生成两个文件，你可以直接打开看（都是普通文本文件）：

### `excluded_review.txt` —— "打算删掉的内容"

列出了 Claude 判断为语气词、重复啰嗦、说错重讲、无关闲聊等**没有保留价值**的内容，但目前还留在成片里。按类型分组列出，方便你一眼看清都是些什么，比如：

```
"哦" x106
"啊" x46
```

如果你觉得都合理，直接跳到第五步告诉 Claude 全部删除即可。如果你想留几个特殊的（比如某句"哦"其实是情绪反应想保留），把那一条记下来，跟 Claude 说明。

这个文件最上面还有一个 **"FLAGGED"（标红）** 区块 —— 这是 Claude 的自动纠错检查，正常情况下应该是空的。如果这里有内容，说明可能有真正重要的话被误删了，**这个必须告诉 Claude 去核实**，不能直接忽略。

### `included_review.txt` —— "成片实际会说的话"

这是按时间顺序排列的完整台词稿，就是最终视频里会保留下来的所有有意义的对话。**建议从头到尾读一遍**，看看整体内容顺不顺、有没有奇怪的重复。

### Claude 自己也拿不准的内容

如果有些地方 Claude 判断不了该删还是该留（比如某句话算不算废话，它自己也没把握），**它不会自动处理，而是会直接在对话里列出来问你**，附上时间戳、原话和它犹豫的原因。等你确认之后它才会处理——这类内容不会悄悄藏在文件里被自动删掉，一定会当面问你，你说了算。

如果你自己发现有重复啰嗦的地方（比如同一句话说了两三遍），也可以主动把时间戳和文字复制给 Claude：

```
[时间戳] 具体文字

这里我觉得是多余的重复，可以删掉吗？
```

Claude 会告诉你它的判断（是真的口误重复，还是刻意强调、拟声词等不该删的情况），说明理由，你再决定要不要执行。

---

## 第五步：确认执行

看完报告、决定好要删什么之后，跟 Claude 说：

```
可以了，帮我把这些改动应用到剪映项目里
```

Claude 会提醒你：**这一步之前必须先关闭剪映专业版**（剪映开着的话会导致改动被覆盖丢失）。关闭后回复：

```
剪映已经关闭，请继续
```

Claude 会把最终结果写入你的剪映项目，并做多项自动检查确认没有问题。这一步在处理片段较多的项目时可能要花一点时间，Claude 会不时汇报进度（比如"已完成 300/774 段"），不用担心它卡住了。

---

## 第六步：在剪映里检查、导出

1. 重新打开剪映专业版（如果关闭前它是开着的，必须重新打开一次，剪映不会自动感知外部改动）。
2. 打开你的项目，播放检查一下效果。
3. 满意的话，点右上角「导出」，设置分辨率/帧率后确认导出。

---

## 常见问题

**Q: 输入 `code .` 或 `claude` 没反应怎么办？**
说明这台电脑还没配置好开发环境（VS Code / Claude Code 没装好，或者没加到系统路径里）。这是一次性的技术设置，请找负责搭建这套工具的人帮忙按 `SETUP.md` 走一遍，配置好之后就不用再管了，以后每次都是"打开文件夹 → `code .` → 开终端 → `claude`"这四步。

**Q: 每次都要走一遍第三步（字幕辅助）吗？**
不需要。第二步的默认流程已经够用，第三步是想要更干净的成片、或者发现漏剪/错剪时才需要的加强版。

**Q: 第四步里"Claude 自己也拿不准"的内容是什么意思？**
就是 Claude 判断这段内容删不删都有道理、它自己没有十足把握的情况（比如玩笑话算不算多余、某句强调算不算重复）。这类内容它**不会自动删**，而是列出来直接问你，由你来拍板。

**Q: 中途可以随时补充要求吗？**
可以，比如"这几个词也当作语气词处理""这段重复的也删掉"，直接用中文跟 Claude 说就行，不需要按固定格式。

**Q: 如果结果不满意怎么办？**
告诉 Claude 具体哪里不对（最好带时间戳和原话），比如"这句话很重要但被删了"或者"这里剪得太快了"，Claude 会检查原因并重新调整。

**Q: 为什么改动前一定要关闭剪映？**
剪映开着的时候，它会用自己内存里的旧版本内容覆盖磁盘上的文件，导致 Claude 刚写入的改动凭空消失。这是实际验证过的问题，务必照做。
